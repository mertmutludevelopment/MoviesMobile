package com.example.moviesmobile.ui.theme

import androidx.compose.ui.graphics.Color

// Theme colors
val Primary = Color(0xFF6B4EFF)         // Canlı mor - ana renk
val PrimaryVariant = Color(0xFF4E35CC)  // Koyu mor
val Secondary = Color(0xFF00E5FF)       // Turkuaz - vurgu rengi
val Background = Color(0xFF0A0B1E)      // Çok koyu lacivert - arka plan
val Surface = Color(0xFF1A1B3D)         // Koyu lacivert - kartlar için
val Error = Color(0xFFFF4E6E)           // Soft kırmızı - hata durumları

// Text colors
val OnPrimary = Color(0xFFFFFFFF)       // Beyaz
val OnSecondary = Color(0xFF000000)     // Siyah
val OnBackground = Color(0xFFE6E6FF)    // Açık mor-beyaz
val OnSurface = Color(0xFFE6E6FF)       // Açık mor-beyaz
val OnError = Color(0xFF1A1B3D)         // Koyu lacivert

// Additional colors for accents
val AccentGradientStart = Color(0xFF6B4EFF)  // Mor başlangıç
val AccentGradientEnd = Color(0xFF00E5FF)    // Turkuaz bitiş